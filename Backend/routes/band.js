var last_lat;
var last_long;
var new_lat;
var new_long;

if( COMPARA LAST_LAT COM TODOS RAIOS)FILA--
if( COMPARA LAST_LONG COM TODOS RAIOS)FILA--

navigator.geolocation.getCurrentPosition(function(pos) {
            new_lat = pos.coords.latitude;
            new_long.coords.longitude;
        });

if( COMPARA NEW_LAT COM TODOS RAIOS)FILA++
if( COMPARA NEW_LONG COM TODOS RAIOS)FILA++
